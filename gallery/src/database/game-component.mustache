import React from 'react';

import Game from "../../components/game-item"
import primaryLocalImg from '../../images/resize/{{componentName}}.jpg';
import coverLocalImg from '../../images/resize/{{componentName}}-cover.jpg';

const {{componentName}} = () => {
  return (
    <Game
      name="{{name}}"
      releaseYear="{{releaseYear}}"
      igdbURL="{{{igdbURL}}}"
      platformVersion="{{platformVersion}}"
      platformPlayedOn="{{{platformPlayedOn}}}"
      flickrURL="{{flickrURL}}"
      version="{{version}}"
      countPhotos="{{countPhotos}}"
      countVideos="{{countVideos}}"
      primaryLocalImg={primaryLocalImg}
      coverLocalImg={coverLocalImg}
    />
  );
};

export default {{componentName}};