import React from 'react';
import Event from "../../components/event-item"

const {{componentName}} = () => {
  return (
    <Event name="{{name}}" year="{{year}}" country="{{country}}" website="{{{website}}}" photos="{{{link}}}" imgSrc="" />
  );
};

export default {{componentName}};