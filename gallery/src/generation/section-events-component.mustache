import React from 'react';

import Section from '../../components/section';
{{#events}}
import {{.}} from '../{{.}}';
{{/events}}

const SectionEvents = () => {
  return (
    <Section title="{{nbEvents}} Events">
      <div id="events" className="grid md:grid-cols-3 gap-6 xl:gap-x-12 mb-16 text-center lg:text-left">
        {{#events}}
        <{{.}} />
        {{/events}}
      </div>
    </Section>
  );
};

export default SectionEvents;
